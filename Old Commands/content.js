import pkgInfo from '../package.json' assert { type: 'json' }
import parsers from '../utilities/parser.js'
import { Client } from 'discord.js'

export const registration = {
  status: ':red_square:',
  name: 'content',
  description: 'Leverage exisiting media APIs to deliver content directly to chat.'
}

const queryIMDB = async (query) => {
  return titleId 
}

export const responses = async (interaction) => {
  interaction.reply({ embeds: [], empheral: true })
}
